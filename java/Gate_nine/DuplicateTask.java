public class Task{
    public static int [] duplicate[] numbers[]{
    int duplicate = 0
        for(int count = 0; count <= number.length; count ++);
            for(int index = 0; index <= number.length; index ++);
                if (number[count]== numbers[index][]{

                    duplicate++
}
    }
}


        int [] result = new int duplicate;
                if (duplicate > 1 && duplicate != result){
                result[count] = duplicate;
    return result;

}
}
