input = [Student_scores]
    for scores in numbers
        if student scores > 50
    print("pass")
        if student score < 50
     print("Fail")
else
        if student scores < 40
    print(Go and sleep)
        
